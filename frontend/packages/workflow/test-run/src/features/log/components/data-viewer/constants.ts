/** Preset special key */
export enum LogObjSpecialKey {
  Error = '$error',
  Warning = '$warning',
}

/** Display style type of value in log */
export enum LogValueStyleType {
  Default,
  Number,
  Boolean,
}
